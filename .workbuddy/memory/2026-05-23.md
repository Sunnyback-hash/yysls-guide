# 2026-05-23

## Claude Code 配置 DeepSeek API
- 修复了 Claude Code 接入 DeepSeek API 的配置问题
- **根因**: ANTHROPIC_BASE_URL 应为 `https://api.deepseek.com/anthropic`（Anthropic兼容端点），而非 `https://api.deepseek.com`
- 模型更新为 V4 系列：`deepseek-v4-flash`（默认）、`deepseek-v4-pro`（opus/复杂任务）
- 添加了 `API_TIMEOUT_MS: 600000` 和 `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC: 1`
- 旧模型 `deepseek-chat`/`deepseek-reasoner` 将于 2026/07/24 弃用
- Key 已轮换为 `sk-449e...`

## VS Code + Claude Code + CC Switch 迁移到 D 盘
- 目标目录: `D:\AI助理\`
- 使用 Windows Directory Junction 技术实现透明迁移

**已完成的 Junction（全部验证通过）：**
| 源路径 (C:) | → 目标 (D:\AI助理\) | 大小 |
|---|---|---|
| `AppData\Local\Programs\Microsoft VS Code` | `Microsoft VS Code` | 714 MB |
| `\.vscode` | `\.vscode` | 395 MB |
| `\.claude` | `\.claude` | 17 MB |
| `\.cc-switch` | `\.cc-switch` | 0.3 MB |
| `AppData\Local\com.ccswitch.desktop` | `com.ccswitch.desktop` | 70 MB |

**Code 用户数据 Junction — 已完成：**
- `AppData\Roaming\Code` → `D:\AI助理\Code` (259 MB) ✅
- 通过计划任务在重启登录后自动完成
- `Code.orig` 备份已自动清理

**2026-05-24 更新：**
- 重启后 `.vscode`、`.claude`、`.cc-switch` 三个 Junction 丢失，已用 `mklink /J` 重建
- 重建命令：`Start-Process cmd -ArgumentList '/c','mklink','/J',...`  通过 PowerShell 执行
- 最终 6/6 Junctions 全部验证通过

**最终状态：**
- 全部 6 个 Junction 正常工作
- C 盘已释放：~2.5 GB（含 Code.orig 清理）
